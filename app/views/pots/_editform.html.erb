<%= form_with(model: pot) do |form| %>
  <% if pot.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pot.errors.count, "error") %> prohibited this pot from being saved:</h2>

      <ul>
        <% pot.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 
  <div>
    <%= form.label :lesson, style: "display: block" %>
    <%= form.text_field :lesson, autocomplete: "lesson", class:'form-control', value: params[:lesson], value: pot.lesson %>

  </div>

  <div>
    <%= form.label :class_type, style: "display: block" %>
    <%= form.text_field :class_type, autocomplete: "class_type", class:'form-control', value: params[:class_type], value: pot.class_type %>
  </div>

  <div>
   
    <%= form.text_field :email, autocomplete: "email", class:'form-control', value: current_user.email, type: :hidden %>
  </div>

  <div>   
    <%= form.number_field :user_id, class:'form-control', id: :pot_user_id, value: current_user.id, type: :hidden %>
  </div>
  <br/>
  <div>
    
    <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
 Edit Class
<% end %>
  </div>
<% end %>
